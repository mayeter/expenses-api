PORT=8080
DATABASE_URL=postgres://expenses:expenses@localhost:5432/expenses?sslmode=disable
# Comma-separated origins; * is allowed for MVP
ALLOWED_ORIGINS=http://localhost:3000
# Next BFF sends X-Internal-Secret matching this value. Leave empty to skip internal auth (local Go-only).
INTERNAL_API_SECRET=
